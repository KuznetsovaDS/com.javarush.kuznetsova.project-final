spring:
  cache:
    type: none
  sql:
    init:
      mode: never
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;NON_KEYWORDS=VALUE;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  liquibase:
    changeLog: "classpath:changelog-h2.sql"